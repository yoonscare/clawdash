# Character avatars

Place uploaded character images here with these filenames:

- `claw.png`
- `clawau.png`
- `clovi.png`

If an image exists, ClawDash will use it first.
If it does not exist or fails to load, the app falls back to the built-in SVG avatar.
